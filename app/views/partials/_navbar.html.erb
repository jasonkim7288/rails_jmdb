<div id="nav-margin"></div>

<nav class="main-width mx-auto px-5 fixed-top navbar navbar-expand-lg navbar-dark" id="nav-id" >
  <a class="navbar-brand btn btn-warning btn-sm font-weight-bold" href=<%= root_path %>>JMDb</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <%= form_with url: search_movie_path, method: :get, class: "form-inline", local: true do |f| %>
          <div id= "search-data-movie" class="form-search search-data input-group" data-names="">
            <%= text_field_tag :search_text_movie, nil, autocomplete: :off, autofocus: :true, class: "form-control search-input my-1 my-md-0", style: "min-width: 450px; max-width: 450px", placeholder: "Search JMDb", area: {describedby: "button-addon"} %>
            <div class="input-group-append">
              <%= submit_tag "Search", class: "btn btn-outline-secondary button-home", id: "button-addon" %>
            </div>
          </div>
        <% end %>
      </li>
    </ul>

    <ul class="navbar-nav ml-4">
        <% if user_signed_in? %>
            <li class="nav-item">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <%= current_user.full_name %>
                    </a>
                    <div class="dropdown-menu dropdown-dark" aria-labelledby="navbarDropdown">
                        <%= link_to 'My Movies', movies_mymovies_path, class: "dropdown-item dropdown-dark" %>
                        <%= link_to 'My Ratings', movies_myratings_path, class: "dropdown-item dropdown-dark" %>
                        <div class="dropdown-divider"></div>
                        <%= link_to 'Log out', destroy_user_session_path, method: :delete, class: "dropdown-item" %>
                    </div>
                </li>
            </li>
        <% else %>
            <li class="nav-item">
                <%= link_to 'Log in', new_user_session_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
                <%= link_to 'Sign up', new_user_registration_path, class: "nav-link" %>
            </li>
        <% end %>
    </ul>
  </div>
</nav>

